[ -n "$HOTSPOT_NAME" ] || HOTSPOT_NAME=RatOS
[ -n "$HOTSPOT_PASSWORD" ] || HOTSPOT_PASSWORD=raspberry
[ -n "$HOTSPOT_CHANNEL" ] || HOTSPOT_CHANNEL=6
[ -n "$HOTSPOT_ADDRESS" ] || HOTSPOT_ADDRESS=192.168.50.1/24
[ -n "$HOTSPOT_CHECK_INTERVAL" ] || HOTSPOT_CHECK_INTERVAL=30
